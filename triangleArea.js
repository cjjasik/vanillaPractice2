/**
create a function that will return the area of a triangle given base & height in integers.
 */


function triangleArea (base, height) {
    // a = 1/2bh
    let area = (1/2) * base * height;

    console.log(area);
}

triangleArea(5, 10);